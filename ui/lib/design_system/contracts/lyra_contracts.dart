// SPDX-FileCopyrightText: 2026 Tzu-Ting Lin
// SPDX-License-Identifier: AGPL-3.0-or-later

/// Button styling variants supported by the Lyra Design System.
enum LyraButtonVariant { primary, secondary, outline, ghost, destructive, link }

/// Button size options.
enum LyraButtonSize { sm, md, lg }

/// Badge styling variants for tags and statuses.
enum LyraBadgeVariant {
  defaultVariant,
  secondary,
  outline,
  destructive,
  success,
}

/// Card container styling variants.
enum LyraCardVariant { defaultVariant, outlined, elevated }
